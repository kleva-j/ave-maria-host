# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Environment
NODE_ENV="development"

# Database Reset Safety (DESTRUCTIVE - Use with caution!)
# Uncomment and set to 1 to allow database reset operations
# DB_RESET_CONFIRM=1

# Production Reset Override (EXTREMELY DANGEROUS!)
# Only set this in production if you absolutely need to reset production data
# DB_RESET_ALLOW_PRODUCTION=1
