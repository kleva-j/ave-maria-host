// Shared Error Definitions
// Common error types used across the application

// Placeholder for shared errors - will be implemented in subsequent tasks
export type SharedError = {
  // This will be populated with actual error definitions using Effect's tagged errors
};
