// Shared Type Definitions
// Common types used across the application

// Placeholder for shared types - will be implemented in subsequent tasks
export type SharedType = {
  // This will be populated with actual shared type definitions
};
