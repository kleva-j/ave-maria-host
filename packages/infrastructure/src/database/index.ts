// Database Infrastructure
// Concrete implementations of repository interfaces using Drizzle ORM

// Placeholder for database implementations - will be implemented in subsequent tasks
export type DatabaseImplementation = {
  // This will be populated with actual repository implementations like DrizzleSavingsRepository, etc.
};
